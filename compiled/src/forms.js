"use strict";const DEFAULT_FORM_CONFIG={submitUrl:"https://ebj-forms.tim-dee.workers.dev/submit",maxAttachmentBytes:10*1024*1024,maxTotalBytes:20*1024*1024,requestTimeoutMs:20000,retryAttempts:3,retryBackoffMs:1000};let formConfigPromise;function getFormConfig(){if(!formConfigPromise){formConfigPromise=fetch("/form-config.json",{cache:"no-store"}).then(response=>{if(!response.ok)throw new Error(`Form config ${response.status}`);return response.json()}).catch(error=>{console.warn("Using default form configuration:",error);return{default:DEFAULT_FORM_CONFIG,roles:{}}})}return formConfigPromise}function mergeFormConfig(config,formKey){const defaults={...DEFAULT_FORM_CONFIG,...(config?.default||{})};const roleConfig=formKey&&config?.roles?.[formKey]?config.roles[formKey]:{};return{...defaults,...roleConfig}}function formatBytes(bytes){if(!Number.isFinite(bytes))return"";const units=["B","KB","MB","GB"];const magnitude=Math.min(Math.floor(Math.log(Math.max(bytes,1))/Math.log(1024)),units.length-1);const value=bytes/1024**magnitude;return`${value.toFixed(value>=10||magnitude===0?0:1)} ${units[magnitude]}`}function validateFormFiles(form,settings){const maxAttachmentBytes=Number(settings.maxAttachmentBytes)||0;const maxTotalBytes=Number(settings.maxTotalBytes)||0;let totalBytes=0;const oversized=[];form.querySelectorAll("input[type=\"file\"]").forEach(input=>{Array.from(input.files||[]).forEach(file=>{totalBytes+=file.size;if(maxAttachmentBytes&&file.size>maxAttachmentBytes){oversized.push(`${file.name} (${formatBytes(file.size)})`)}})});if(oversized.length){return{valid:false,message:`Each file must be ${formatBytes(maxAttachmentBytes)} or less. Please replace: ${oversized.join(", ")}.`}}if(maxTotalBytes&&totalBytes>maxTotalBytes){return{valid:false,message:`Combined attachments must be ${formatBytes(maxTotalBytes)} or less. Your current selection totals ${formatBytes(totalBytes)}.`}}return{valid:true}}function useManagedForm({formKey,successMessage,errorMessage,submittingMessage}){const[status,setStatus]=React.useState({message:"",type:""});const[submitting,setSubmitting]=React.useState(false);const submitForm=React.useCallback(async event=>{event.preventDefault();const form=event.currentTarget;const config=await getFormConfig();const settings=mergeFormConfig(config,formKey);const endpoint=settings.submitUrl;if(!endpoint){setStatus({message:errorMessage,type:"error"});return}const validation=validateFormFiles(form,settings);if(!validation.valid){setStatus({message:validation.message,type:"error"});return}const attempts=Number(settings.retryAttempts)||1;const timeoutMs=Number(settings.requestTimeoutMs)||20000;const retryBackoffMs=Number(settings.retryBackoffMs)||750;const formData=new FormData(form);setSubmitting(true);setStatus({message:submittingMessage,type:"info"});for(let attempt=0;attempt<attempts;attempt+=1){const controller=new AbortController;const timer=window.setTimeout(()=>controller.abort(),timeoutMs);try{const response=await fetch(endpoint,{method:"POST",body:formData,headers:{Accept:"application/json"},signal:controller.signal});if(!response.ok){let detail="";try{detail=(await response.json()).error||""}catch{detail=""}throw new Error(detail||`Submission failed with ${response.status}`)}form.reset();setStatus({message:successMessage,type:"success"});setSubmitting(false);return}catch(error){const canRetry=attempt<attempts-1;if(!canRetry){console.error("Form submission failed:",error);setStatus({message:errorMessage,type:"error"});setSubmitting(false);return}await new Promise(resolve=>window.setTimeout(resolve,retryBackoffMs*2**attempt))}finally{window.clearTimeout(timer)}}},[errorMessage,formKey,submittingMessage,successMessage]);return{submitForm,status,submitting}}Object.assign(window,{useManagedForm,formatFormBytes:formatBytes});